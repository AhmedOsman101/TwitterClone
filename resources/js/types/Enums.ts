export enum ProfileOptions {
	Posts = "posts",
	Likes = "likes",
	Replies = "replies",
}

export enum NotificationTypes {
	Like = "like",
	Reply = "reply",
	Follow = "follow",
}

export enum NotificationOptions {
	All = "all",
	Read = "read",
	Unread = "unread",
}
